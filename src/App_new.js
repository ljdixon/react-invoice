import React, { useState } from 'react';
import Form from './Form';
import './App.css';

export default function App(props) {
    return (
        <div className="app">
            <Form />
        </div>
    );
}